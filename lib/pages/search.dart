import 'package:flutter/material.dart';

import '../util/widgets/custom_text.dart';

class Search extends StatelessWidget {
  const Search({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CustomText(
        title: 'S E A R  C H -  P A G E',
      ),
    );
  }
}
