import 'package:flutter/material.dart';

import '../util/widgets/custom_text.dart';

class Profile extends StatelessWidget {
  const Profile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CustomText(
        title: 'P R O F I L E -  P A G E',
      ),
    );
  }
}
